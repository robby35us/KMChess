package definitions;

/*
 * Symbolizes a file, or column, on the chess board.
 * A signifies the far left file, from the white 
 * player's perspective, and H represents the far
 * right.
 */
public enum File {
	A, B, C, D, E, F, G, H
}
